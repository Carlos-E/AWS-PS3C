# version: 0.2

# phases:
#   build:
#     commands:
#       - mvn compile war:war
#   post_build:
#     commands:
#       - mv target/ps3c.war ./
# artifacts:
#   packaging: NONE
#   files:
#     - ps3c.war

version: 0.2

phases:
  build:
    commands:
      - mvn compile war:war
  post_build:
    commands:
      - mv target/my-web-app.war my-web-app.war
      - ls
artifacts:
  files:
    - my-web-app.war
    - .ebextensions/**/*